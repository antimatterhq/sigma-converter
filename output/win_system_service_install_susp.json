{
  "metadata": {
    "name": "Suspicious Service Installation",
    "comment": "Detects suspicious service installation commands",
    "annotations": {
      "source": "sigma",
      "product": "windows",
      "service": "system",
      "sigma_status": "test"
    }
  },
  "spec": {
    "schedule": {
      "atLeastEvery": "15m",
      "enabled": true
    },
    "input": {
      "batch": {
        "sql": "SELECT * FROM process_activity WHERE time BETWEEN CURRENT_TIMESTAMP() - INTERVAL 24 HOUR AND CURRENT_TIMESTAMP() AND (lower(metadata.log_provider) = lower('service control manager') AND metadata.event_code = 7045 AND (contains(lower(process.file.path), lower(' -nop ')) OR contains(lower(process.file.path), lower(' -sta ')) OR contains(lower(process.file.path), lower(' -w hidden ')) OR contains(lower(process.file.path), lower(':\\\\temp\\\\')) OR contains(lower(process.file.path), lower('.downloadfile(')) OR contains(lower(process.file.path), lower('.downloadstring(')) OR contains(lower(process.file.path), lower('\\\\admin$\\\\')) OR contains(lower(process.file.path), lower('\\\\perflogs\\\\')) OR contains(lower(process.file.path), lower('&&'))))"
      }
    },
    "output": {
      "summary": "Detects suspicious service installation commands",
      "defaultContext": true
    },
    "metadata": {
      "version": 1.0,
      "severity": "High",
      "fidelity": "Investigative",
      "objective": "False positive: Unknown Reference: Internal Research",
      "mitre": [
        {
          "taxonomy": "MITRE ATT&CK",
          "tactic": "Persistence",
          "techniqueId": "T1543",
          "subTechniqueId": "003"
        }
      ]
    },
    "computeMode": "high"
  },
  "apiVersion": "v1",
  "kind": "Rule"
}